/*
	//头部导航栏的展开收回
	var isExtend=false;
	function navFadeToggle(){		
		$("#OCXBody").stop(true,false).fadeToggle();
		$(".cover").stop(true,false).fadeToggle();
		$(".nav-list").stop(true,false).slideToggle();

	}
	$(".nav-img").click(function(){
		if(isExtend){
			$(this).attr("src","images/common/navup.png");
			isExtend=false;
		}else{
			$(this).attr("src","images/common/navdown.png");
			isExtend=true;
		}
		navFadeToggle();
	})
	$(".cover").click(function(){
		navFadeToggle();
	})


	//机器人路线，画线

	//draw()调用在real-video.js文件的window.onload里
	function draw(){
		var c=document.getElementById("draw-line");
		var cxt=c.getContext("2d");
		cxt.beginPath();
		cxt.moveTo(68,255);
		cxt.lineTo(68,188);	
		cxt.lineTo(480,188);	
		cxt.lineTo(480,105);
		cxt.lineTo(550,105);
		cxt.strokeStyle="black";
		cxt.lineWidth=3;	
		cxt.stroke();
		cxt.closePath();
	}	

	//贝塞尔曲线图
	// cxt.beginPath();
	// cxt.moveTo(65,255);
	// cxt.bezierCurveTo(65,255,65,170,80,170);
	// cxt.bezierCurveTo(80,170,180,170,280,170);
	// cxt.strokeStyle="red";
	// cxt.lineWidth=3;	
	// cxt.stroke();
	// cxt.closePath();

	//机器人运动
	function robot(){
		var c=document.getElementById("draw-line");
		var cxt=c.getContext("2d");
		cxt.beginPath();
		cxt.moveTo(robotSiteX,robotSiteY);
		if(robotSiteY>188){
		  robotSiteY=robotSiteY-1;
		}else{
			robotSiteX=robotSiteX+1;
		}

		cxt.lineTo(robotSiteX,robotSiteY);
		cxt.strokeStyle="#0f0";
		cxt.lineWidth=4;	
		cxt.stroke();
		cxt.closePath();
		$(".robot").css({"left":robotSiteX,"top":robotSiteY}); //改变机器人位置
	}
	var robotSiteX=68,robotSiteY=255;
	var self=setInterval("robot()",1000);



$(function(){
			//定义数据表格数据字段，样式，导入数据
            $("#maingrid").ligerGrid({
                columns: [
                { display: '识别时间', name: 'time', align: 'center', width: "20%"},
                { display: '机器人名称', name: 'robot', Width: "25%" },
                { display: '点位名称', name: 'site', Width: "20%" },
                { display: '识别结果', name: 'result',width:"20%" },
                { display: '告警等级', name: 'grade',width:"15%" }
                ], dataAction: 'server', data: CustomersData, sortName: 'CustomerID',
                width: '98%', height: '261px', pageSize: 10,rownumbers:true,rowtitle:true,
                checkbox : false,
                //应用灰色表头
                cssClass: 'l-grid-gray2014', 
                heightDiff: -6
            });
            $("#secondgrid").ligerGrid({
                columns: [
                { display: '识别时间', name: 'time', align: 'center', width: "30%"},
                { display: '机器人名称', name: 'robot', Width: "30%" },
                { display: '点位名称', name: 'site', Width: "30%" },
                ], dataAction: 'server', data: CustomersData, sortName: 'CustomerID',
                width: '98%', height: '261px', pageSize: 10,rownumbers:true,rowtitle:true,
                checkbox : false,
                //应用灰色表头
                cssClass: 'l-grid-gray2014', 
                heightDiff: -6
            });
            $("#thirdgrid").ligerGrid({
                columns: [
                { display: '识别结果', name: 'result',width:"50%" },
                { display: '告警等级', name: 'grade',width:"50%" }
                ], dataAction: 'server', data: CustomersData, sortName: 'CustomerID',
                width: '98%', height: '261px', pageSize: 10,rownumbers:true,rowtitle:true,
                checkbox : false,
                //应用灰色表头
                cssClass: 'l-grid-gray2014', 
                heightDiff: -6
            });  
            gridManager = $("#maingrid").ligerGetGridManager();
 			gridManager = $("#secondgrid").ligerGetGridManager();
 			gridManager = $("#thirdgrid").ligerGetGridManager();
            $("#pageloading").hide();

            //控制标题单击事件显示不同的表格数据
			$(".info .title span").click(function(){
				var spanIndex=$(".info .title span").index(this);
				$(".info .title span").removeClass("active").eq(spanIndex).addClass("active");
				$(".info .grid").hide().eq(spanIndex).show();
				//$(".l-bar-btnload").click(); //重载数据
			})
})

*/